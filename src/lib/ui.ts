/** Bayangan lembut bersama — didukung RN 0.76+ (boxShadow) dan web. */
export const CARD_SHADOW = {
  boxShadow: "var(--shadow-card)",
  transition: "box-shadow 0.3s ease",
} as const;

export const HERO_SHADOW = {
  boxShadow: "var(--shadow-hero)",
  transition: "box-shadow 0.3s ease",
} as const;
